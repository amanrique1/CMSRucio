apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "unsuspend-rules-monit-chart.fullname" . }}-test-connection"
  labels:
    {{- include "unsuspend-rules-monit-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "unsuspend-rules-monit-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
